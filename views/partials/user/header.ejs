<!doctype html>
<html lang="zxx">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Sit Well</title>
    <link rel="icon" href="/user/img/sitwell.png" type="image/png">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/user/css/bootstrap.min.css">
    <!-- animate CSS -->
    <link rel="stylesheet" href="/user/css/animate.css">
    <!-- owl carousel CSS -->
    <link rel="stylesheet" href="/user/css/owl.carousel.min.css">
    <!-- font awesome CSS -->
    <link rel="stylesheet" href="/user/css/all.css">
    <!-- flaticon CSS -->
    <link rel="stylesheet" href="/user/css/flaticon.css">
    <link rel="stylesheet" href="/user/css/themify-icons.css">
    <!-- font awesome CSS -->
    <link rel="stylesheet" href="/user/css/magnific-popup.css">
    <!-- swiper CSS -->
    <link rel="stylesheet" href="/user/css/slick.css">
    <!-- style CSS -->
    <link rel="stylesheet" href="/user/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</head><style>
    .navbar-brand img {
    height: 50px; 
    width: auto; 
    transition: all 0.3s ease; 
}
    .navbar-nav .nav-link {
        color: #333;
        font-weight: 500;
    }
    .navbar-nav .nav-link:hover {
        color: #007bff;
    }
    .header-icons a {
        color: #333;
        font-size: 1.2rem;
        margin-left: 15px;
    }
    .header-icons a:hover {
        color: #007bff;
    }
    @media (max-width: 991.98px) {
        .header-icons {
            margin-top: 15px;
        }
        .navbar-brand {
            position: absolute;
            right: 50px;
            top: 20%;
            transform: translateY(-50%);
        }
        .navbar-brand img {
            transform: translateY(-50%);
            height: 40px;
            margin: 0;
        }
        .navbar-toggler {
            margin-left: 15px;
        }
    }
    @media (max-width: 768px) {
    .navbar-brand img {
        height: 40px; /* Smaller height for mobile screens */
        width: auto; /* Keep aspect ratio */
        margin-left: 0; /* Align to the left if necessary */
    }
    
    .navbar .container {
        display: flex;
        justify-content: space-between; /* Proper alignment of elements */
        align-items: center;
    }
}
</style>

<body>
    <!--::header part start::-->
    <header class="main_menu home_menu">
        <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-2">
            <div class="container">
                <a class="navbar-brand " href="/">
                    <img src="/user/img/sitwell.png" alt="Sitwell Logo" style="height: 40px; width: 60px;">
                </a>
    
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            
                <div class="collapse navbar-collapse" id="navbarContent">
                    <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link fw-bold" href="/" style="color: #333; transition: color 0.3s;">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link fw-bold" href="/shop" style="color: #333; transition: color 0.3s;">Shop</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link fw-bold" href="/about" style="color: #333; transition: color 0.3s;">About</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link fw-bold" href="/contact" style="color: #333; transition: color 0.3s;">Contact As</a>
                        </li>
                    </ul>
                    
                    <div class="d-flex align-items-center">
                        <% if (locals.user) { %>
                            <div class="dropdown">
                                <button class="btn btn-outline-primary dropdown-toggle" type="button" id="userMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="border-radius: 20px; padding: 8px 16px;">
                                    <span class="user-avatar bg-primary text-white rounded-circle d-inline-flex align-items-center justify-content-center me-2" style="width: 30px; height: 30px;">
                                        <%= locals.user.name.charAt(0).toUpperCase() %>
                                    </span>
                                    <span class="d-none d-md-inline"><%= locals.user.name %></span>
                                </button>
                                <div class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userMenuButton" style="border: none; border-radius: 10px;">
                                    <a class="dropdown-item py-2" href="/userProfile"><i class="fa fa-user me-2 "></i>Profile</a>
                                    <a class="dropdown-item py-2" href="/order"><i class="fa fa-shopping-bag me-2 "></i>Orders</a>
                                    <a class="dropdown-item py-2" href="/Whishlish"><i class="fa fa-heart me-2 "></i>Wishlist</a>
                                    <a class="dropdown-item py-2" href="/cart"><i class="fa fa-shopping-cart me-2 "></i>Cart</a>
                                    <a class="dropdown-item py-2" href="/userCoupons"><i class="fa fa-ticket-alt me-2 "></i>Coupons</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item py-2" href="/logout"><i class="fas fa-sign-out-alt me-2 "></i>Logout</a>
                                </div>
                            </div>
                        <% } else { %>
                            <a href="/login" class="btn btn-outline-secondary" style="border-radius: 20px; padding: 8px 16px;">
                                <i class="fas fa-sign-in-alt me-2"></i>Login
                            </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </nav>
        <div class="search_input" id="search_input_box" style="display: none; background-color: rgba(0,0,0,0.8); padding: 20px 0;">
            <div class="container">
                <form class="d-flex justify-content-between search-inner">
                    <input type="text" class="form-control bg-transparent text-white border-0" id="search_input" placeholder="Search Here" style="flex-grow: 1; margin-right: 10px;">
                    <button type="submit" class="btn btn-primary">Search</button>
                    <span class="text-white ms-3" id="close_search" title="Close Search" style="cursor: pointer;"><i class="fas fa-times"></i></span>
                </form>
            </div>
        </div>
    </header>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const searchInputBox = document.getElementById('search_input_box');
            const searchIcon = document.getElementById('search_icon');  // Assuming there's a search icon element
            const closeSearch = document.getElementById('close_search');
    
            // Show search bar when search icon is clicked
            searchIcon.addEventListener('click', function() {
                searchInputBox.classList.add('active');
            });
    
            // Close search bar when close button is clicked
            closeSearch.addEventListener('click', function() {
                searchInputBox.classList.remove('active');
            });
        });


        function toggleUserMenu(button) {
            const menu = button.closest('.user-menu');
            menu.classList.toggle('active');
            
            // Close menu when clicking outside
            document.addEventListener('click', function closeMenu(e) {
                if (!menu.contains(e.target)) {
                menu.classList.remove('active');
                document.removeEventListener('click', closeMenu);
                }
            });
            }

            // Close menu when pressing Escape key
            document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                document.querySelector('.user-menu.active')?.classList.remove('active');
            }
            });
    </script>
    